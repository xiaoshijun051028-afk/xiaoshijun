# Epic E5 · S5 世界与关卡 (World & Level)

- **系统**：S5 世界与关卡（中枢岛 + 3 浮岛 + 锚点 / 闸门 / 神龛 / 残响节点）
- **依赖**：S0（WorldEnvironment / 渲染）、S2（锚点契约已被消费）、S3（闸门扣池）、S8（神龛位）
- **目标**：兑现支柱 P2（垂直探索）、P3（精美即叙事）、核心循环（探索→战斗→解谜→前进）。
- **出口**：0 逻辑死路；每岛可达比 ≥0.6；神龛覆盖每岛；AC-S5-01..04 覆盖。
- **关联 ADR**：adr-002（闸门扣池）、adr-004（进度随神龛写盘）。
- **GDD 验收覆盖**：AC-S5-01..04（见 `test-plan.md` §7）。

## 故事清单（6）

### ENG-S5-01 · 中枢岛 + 3 浮岛 壳层
- **依据**：world-level.md §②/§⑤（中枢 1 + 浮岛 3–4，v1 取 3）。
- **验收**：
  - [ ] 中枢岛（hub，含神龛与通往各岛锚点链）+ 3 浮岛（战斗区/解谜区/残响节点区）。
  - [ ] 区域难度递进：越远岛屿 telegraph 越短、密度越高（岛 N telegraph ×(1-0.1·N)）。
  - [ ] 用 `resources/env/base_env.tres`（S0-ENG-08）作为世界环境。
- **测试路径**：`tools/level_scan.gd`（结构校验）。
- **估算**：L ｜ **优先级 🟡**

### ENG-S5-02 · 锚点布置（喂给 S2）
- **GDD §⑤**：锚点密度 每 1000 m³ ≥1（P2 可达比）。
- **验收**：
  - [ ] 每岛布置 `grapple_anchor`，经 `AnchorSource` 契约暴露给 S2（S2-ENG-04 读取）。
  - [ ] 密度满足每 1000 m³ ≥1；可抓锚点描边 = `ColorTokens.RESONANCE_GLOW`（青白发光态），不可抓 = `ColorTokens.INACTIVE`（灰）。CONCERN-A 已收口于 `design/color-tokens.md` v1.0，无待裁决占位。
  - [ ] 多锚点重叠：S2 侧优先最近且视线无遮挡（契约已定）。
- **测试路径**：`tools/level_scan.gd`（锚点密度统计）。
- **估算**：M ｜ **优先级 🔴**（S2  locomotion 依赖真实锚点）

### ENG-S5-03 · 共鸣闸门（扣 30）
- **GDD §⑦ / §③**：S5→S3 闸门扣 `GATE_COST=30`；不够不解锁。
- **验收**：
  - [ ] 闸门触发 `ResonancePool.try_spend(30,"gate")`；成功则解锁区域/捷径。
  - [ ] 池不足则闸门不解锁并提示（emit `resonance_spend_rejected`）。
  - [ ] 闸门卡关：池不足且有唯一通路 → 提示绕路/回神龛回充（不卡死）。
- **测试路径**：`tests/integration/test_gate_lock.gd`（池=30 解锁；池=29 不解锁）。
- **估算**：S ｜ **优先级 🔴**（依赖 S3-ENG-01/02）

### ENG-S5-04 · 神龛位 + 复活落点
- **GDD §②/§⑦**：每岛 ≥1 神龛（中枢 1）；复活落点有效。
- **验收**：
  - [ ] 每岛 ≥1 神龛节点（中枢 1），位置含有效复活落点。
  - [ ] 神龛为唯一存档/复活点（接口 S5→S8）；S2 越界复活落点指向最近神龛。
  - [ ] 复活落点经 `reset_physics_interpolation()`（adr-004）。
- **测试路径**：`tools/level_scan.gd`（神龛覆盖断言）+ `tests/integration/test_fall_respawn.gd`（S2-ENG-05 联动）。
- **估算**：S ｜ **优先级 🔴**（依赖 S8-ENG-02）

### ENG-S5-05 · 残响节点位（喂给 S7）
- **GDD §②**：每岛 2–3 残响节点，触发 S7 叙事 + 共鸣 +10。
- **验收**：
  - [ ] 每岛 2–3 节点，位置经 `AnchorSource`/节点服务暴露给 S7（S7-ENG-01 读取与触发）。
  - [ ] 节点 5s cd 逻辑在 S3（S3-ENG-03），本 Story 仅布置位置与触发绑定。
- **测试路径**：`tools/level_scan.gd`（节点计数）。
- **估算**：S ｜ **优先级 🟡**（依赖 S7-ENG-01）

### ENG-S5-06 · 关卡校验工具（可达性 / 死路）
- **GDD §⑦**：AC-S5-01（可达比≥0.6）、AC-S5-02（每岛纯空中路线）、AC-S5-03（0 死路）、AC-S5-04（神龛覆盖）。
- **验收**：
  - [ ] `tools/level_scan.gd`：0 逻辑死路（自动化可达性扫描，发布前 0 死路）。
  - [ ] 每岛可达高点数/体积比 ≥0.6（与 S2-ENG-06 共用）。
  - [ ] 每岛 ≥1 纯空中路线绕敌（与 S2-ENG-06 共用）。
  - [ ] 神龛覆盖每岛 + 复活落点有效（与 ENG-S5-04 共用）。
  - [ ] 接入 CI 每夜任务（control-checklist §K）。
- **测试路径**：`tools/level_scan.gd`（CI 每夜跑，失败报警）。
- **估算**：M ｜ **优先级 🟡**（依赖 S2/S8 内容就绪）

## 跨 Epic 接口（systems-index §3）
- → S2：提供锚点坐标（契约）。
- → S3：闸门扣 30。
- → S8：神龛位 + 复活落点。
- → S7：残响节点位 + 触发。
- → S6：小地图/区域名。
